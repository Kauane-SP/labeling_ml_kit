package com.example.mlkitlabeler

import androidx.camera.core.ImageAnalysis
import androidx.camera.core.ImageProxy

class AnalyzerProviderImpl: ImageAnalysis.Analyzer {
    override fun analyze(image: ImageProxy) {
        if (image != null) {

        }
    }
}